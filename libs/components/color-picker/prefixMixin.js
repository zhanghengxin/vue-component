import { prefix } from '../../utils/common'
export default {
    data () {
        return {
            prefixCls: `${prefix}color-picker`,
            inputPrefixCls: `${prefix}color-picker-input-mix`,
            iconPrefixCls: `${prefix}icon`,
            transferPrefixCls: `${prefix}transfer`
        }
    }
}
